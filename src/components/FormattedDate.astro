---
interface Props {
	date: Date;
}

const { date } = Astro.props;
console.log("==========",date);
---
<p>{date}</p>
<!-- <time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time> -->
